#!/bin/sh

# Run it as curl
FILE="bmatheny-dotfiles.tar.gz";
DIR="~/tmp";
FULL_FILE="${DIR}/${FILE}";

mkdir ${DIR};
rm -f ${FULL_FILE};
wget -O ${FULL_FILE} http://github.com/bmatheny/dotfiles/tarball/master
